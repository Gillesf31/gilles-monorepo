# rituel-model

Pure types and date calculations for Rituel recurring routines. Dates are
calendar-only `YYYY-MM-DD` strings, so they never shift when the browser
converts a timestamp across time zones.

## Running unit tests

Run `pnpm nx test rituel-model` to execute the pure unit tests via
[Vitest](https://vitest.dev/).
