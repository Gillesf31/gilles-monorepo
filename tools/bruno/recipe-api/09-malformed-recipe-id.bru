meta {
  name: Malformed recipe ID
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/recipes/1
  body: none
  auth: none
}

tests {
  test("Returns a sanitized 404", () => {
    expect(res.getStatus()).to.equal(404);
    expect(res.getBody()).to.deep.equal({ message: "Recipe not found" });
  });
}
