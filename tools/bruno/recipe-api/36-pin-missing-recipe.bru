meta {
  name: Pin missing recipe
  type: http
  seq: 36
}

patch {
  url: {{baseUrl}}/recipes/{{missingRecipeId}}/pin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "isPinned": true
  }
}

tests {
  test("Returns 404", () => {
    expect(res.getStatus()).to.equal(404);
    expect(res.getBody()).to.deep.equal({ message: "Recipe not found" });
  });
}
