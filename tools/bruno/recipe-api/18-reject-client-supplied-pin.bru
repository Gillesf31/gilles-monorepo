meta {
  name: Reject client supplied pin
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/recipes
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Bruno omelette",
    "ingredients": [
      {
        "name": "eggs",
        "quantity": "2",
        "unit": ""
      }
    ],
    "instructions": [
      "Beat the eggs.",
      "Cook gently."
    ],
    "isPinned": true
  }
}

tests {
  test("Rejects the request with a sanitized 400", () => {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody()).to.deep.equal({ message: "Invalid recipe" });
  });
}
