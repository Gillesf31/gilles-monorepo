# Use the same restricted role/password/database as .env, with the Docker hostname.
# Apply migrations and provision the recipe_api password first (infra/recipe/README.md).
DATABASE_URL=postgresql://recipe_api:<url-encoded-password>@postgres:5432/recipe
