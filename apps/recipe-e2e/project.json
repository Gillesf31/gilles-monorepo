{
  "name": "recipe-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/recipe-e2e/src",
  "implicitDependencies": ["recipe"],
  "// targets": "to see all targets run: nx show project recipe-e2e --web",
  "targets": {
    "e2e-offline": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/recipe-e2e",
        "command": "playwright test --config=playwright.offline.config.ts"
      }
    }
  }
}
